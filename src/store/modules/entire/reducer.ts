const initalState = {
    currentPage: 0
}
const reducer = 
(state = initalState, action: { type: string }) => {
    switch (action.type) {
        default:
            return state;
    }
}
export default reducer;